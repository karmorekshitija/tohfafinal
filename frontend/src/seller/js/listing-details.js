/**
 * Tohfa Seller Studio — Listing Details Form Handler
 */
document.addEventListener('DOMContentLoaded', () => {
  const saveDraftBtn = document.getElementById('header-save-draft-btn');
  const nextStepBtn = document.getElementById('header-next-step-btn');

  saveDraftBtn?.addEventListener('click', () => {
    alert('Listing draft saved!');
  });

  nextStepBtn?.addEventListener('click', () => {
    // Navigate or trigger next tab
  });
});
